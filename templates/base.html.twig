<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block aside %}
            <ul>
                <li><a href="{{ path('accueil') }}">Accueil</a></li>
                <li><a href="{{ path('creer_article') }}">Créer Article</a></li>
                <li><a href="{{ path('contactCity', {'city': 'Montpellier' }) }}">Contact Montpellier</a></li>
                <li><a href="{{ path('contactCity', {'city': 'Béziers' }) }}">Contact Béziers</a></li>
            </ul>
        {% endblock %}
        {% block body %}{% endblock %}
    </body>
</html>
